import Component from '@glimmer/component';

interface TableReceiversArgs {
  receivers: any[],
  selectReceiver: any,
  closePanel: any
}

export default class TableReceivers extends Component<TableReceiversArgs> {
}
